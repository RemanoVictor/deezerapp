import React from "react";

const TrackTable = ({ title, artist, time }) => {
  <div className="trackDetails">
    <li>{title}</li>
    <li>{artist}</li>
    <li>{time}</li>
  </div>;
};

export default TrackTable;
